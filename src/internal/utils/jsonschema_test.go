package utils_test

import "testing"

func TestJsonschemaUtils(t *testing.T) {
	t.Parallel()

	t.Run("isJsonType", func(t *testing.T) {
		t.Parallel()

		t.Run("should return true for valid json types", func(t *testing.T) {
			t.Parallel()

		})

	})
}
